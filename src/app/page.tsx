// Placeholder — the provider board (send comparison) is next session's work.
export default function Home() {
  return (
    <main className="mx-auto max-w-md px-5 py-16">
      <h1 className="display text-3xl font-extrabold tracking-tight">
        Korido<span className="text-mango">.</span>
      </h1>
      <p className="mt-2 text-sm text-ink-soft">
        Every way to send money home — compared. Launching soon.
      </p>
    </main>
  );
}
